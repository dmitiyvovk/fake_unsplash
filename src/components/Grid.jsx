import React from 'react';

function Grid(props){
    // let results = props.images.results;   results[1].urls.small     results[7].urls.small
    // let urls = results.map(item => item.urls.small); 
    
    
    return (
        <div>
        {/* <p>gello {console.log(props.images)} </p> */}
        </div>
    );
};

export default Grid;